name: Megaweave CI/CD

on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  detect-changes:
    name: Detect Changes
    runs-on: self-hosted
    outputs:
      server-changed: ${{ steps.changes.outputs.server }}
      client-changed: ${{ steps.changes.outputs.client }}
      any-changed: ${{ steps.changes.outputs.server == 'true' || steps.changes.outputs.client == 'true' }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - uses: dorny/paths-filter@v2
        id: changes
        with:
          filters: |
            server:
              - 'server/**'
            client:
              - 'client/**'

    # 新增：部署前清理空間
  pre-deploy-cleanup:
    name: Pre-Deploy Cleanup
    needs: detect-changes
    if: needs.detect-changes.outputs.any-changed == 'true'
    runs-on: self-hosted
    steps:
      - name: Check disk space
        run: |
          echo "📊 當前磁碟使用："
          df -h /
          USAGE=$(df / | tail -1 | awk '{print $5}' | sed 's/%//')
          echo "磁碟使用率: ${USAGE}%"

          if [ $USAGE -gt 85 ]; then
            echo "⚠️  磁碟使用率超過 85%，開始清理..."
            
            # 清理 npm 快取
            npm cache clean --force
            
            # 清理系統日誌
            sudo journalctl --vacuum-time=3d
            
            # 清理 APT
            sudo apt-get clean
            
            # 清理舊備份（只保留最新 1 個）
            cd ~
            ls -dt megaweave.backup.* 2>/dev/null | tail -n +2 | xargs -r rm -rf
            
            # 清理 GitHub Actions 工作目錄
            find ~/github-runner/actions-runner/_work -mindepth 1 -maxdepth 1 -type d -mtime +3 -exec rm -rf {} \;
            
            # 清理臨時文件
            sudo rm -rf /tmp/npm-* 2>/dev/null || true
            
            echo "✅ 清理完成"
            df -h /
          else
            echo "✅ 磁碟空間充足"
          fi

      - name: Check memory
        run: |
          echo "💾 當前記憶體使用："
          free -h

          SWAP_TOTAL=$(free | grep Swap | awk '{print $2}')
          if [ "$SWAP_TOTAL" != "0" ]; then
            SWAP_USAGE=$(free | grep Swap | awk '{if($2>0) print int(($3/$2)*100); else print 0}')
            if [ $SWAP_USAGE -gt 30 ]; then
              echo "⚠️ Swap 使用率: ${SWAP_USAGE}%，清理快取..."
              sudo sync
              sudo sysctl -w vm.drop_caches=3
              echo "✅ 快取已清理"
              free -h
            fi
          fi

  create-backup:
    name: Create Backup
    needs: [detect-changes, pre-deploy-cleanup]
    if: needs.detect-changes.outputs.any-changed == 'true'
    runs-on: self-hosted
    outputs:
      backup-created: ${{ steps.backup.outputs.backup-created }}
      backup-path: ${{ steps.backup.outputs.backup-path }}
    steps:
      - name: Create production backup
        id: backup
        run: |
          if [ -d ~/megaweave ]; then
            BACKUP_DIR=~/megaweave.backup.$(date +%Y%m%d_%H%M%S)
            cp -r ~/megaweave $BACKUP_DIR
            echo "backup-created=true" >> $GITHUB_OUTPUT
            echo "backup-path=$BACKUP_DIR" >> $GITHUB_OUTPUT
            echo "✅ Backup created at: $BACKUP_DIR"
          else
            echo "backup-created=false" >> $GITHUB_OUTPUT
            echo "⚠️ No existing production directory to backup"
          fi

      - name: Cleanup old backups
        run: |
          cd ~
          # 保留最近 2 個備份，刪除更舊的
          ls -dt megaweave.backup.* 2>/dev/null | tail -n +3 | xargs -r rm -rf
          echo "🧹 Cleaned up old backups (keeping last 2)"

  deploy-server:
    name: Deploy Backend
    needs: [detect-changes, create-backup]
    if: always() && 
    (github.event_name == 'workflow_dispatch' || needs.detect-changes.outputs.server-changed == 'true')
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install server dependencies
        run: |
          cd server
          npm ci

      - name: Run server tests
        run: |
          cd server
          if npm run test --if-present; then
            echo "✅ Server tests passed or not configured"
          else
            echo "⚠️ Server tests failed"
            exit 1
          fi

      - name: Stop backend service
        run: |
          sudo systemctl stop megaweave-be.service
          echo "🛑 Backend service stopped"

      - name: Deploy server to production
        run: |
          # 創建生產目錄
          mkdir -p ~/megaweave/server

          # 同步代碼到生產環境
          rsync -av --delete \
            --exclude=node_modules \
            --exclude=.git \
            --exclude=.github \
            --exclude=*.log \
            --exclude=.env.example \
            --exclude=.env.production \
            --exclude=coverage \
            ./server/ ~/megaweave/server/

          echo "📦 Server code deployed"

      - name: Install all dependencies for build
        run: |
          cd ~/megaweave/server
          npm ci

          # Make sure .env file exists
          if [ -f .env.production ]; then
            echo "✅ .env.production found for server"
          else
            echo "❌ .env.production not found!"
            exit 1
          fi
          echo "📥 All dependencies installed for build"

      - name: Build server in production
        run: |
          cd ~/megaweave/server
          npm run build
          echo "📦 Server built successfully in production"

      - name: Install production dependencies only
        run: |
          cd ~/megaweave/server
          # Build 完成後，重新安裝只包含 production 依賴
          npm ci --omit=dev
          echo "🧹 Cleaned up dev dependencies for production runtime"

      - name: Start backend service
        run: |
          sudo systemctl start megaweave-be.service
          echo "🚀 Backend service started"

      - name: Verify backend service
        run: |
          sleep 5
          if sudo systemctl is-active --quiet megaweave-be.service; then
            echo "✅ Backend service is running successfully"
          else
            echo "❌ Backend service failed to start"
            # 顯示服務狀態和日誌
            sudo systemctl status megaweave-be.service
            sudo journalctl -u megaweave-be.service --lines=20 --no-pager
            exit 1
          fi

  deploy-client:
    name: Deploy Frontend
    needs: [detect-changes, create-backup]
    if: needs.detect-changes.outputs.client-changed == 'true'
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install client dependencies
        run: |
          cd client
          npm ci --prefer-offline --no-audit

      - name: Run client tests
        run: |
          cd client
          echo "⏭️ Tests skipped (temporarily disabled)"

      - name: Stop frontend service
        run: |
          sudo systemctl stop megaweave-fe.service
          echo "🛑 Frontend service stopped"

      - name: Deploy client to production
        run: |
          # 創建生產目錄
          mkdir -p ~/megaweave/client

          # 同步代碼到生產環境
          rsync -av --delete \
            --exclude=node_modules \
            --exclude=.git \
            --exclude=.github \
            --exclude=.next \
            --exclude=*.log \
            --exclude=.env.local \
            --exclude=.env.example \
            --exclude=.env.production \
            --exclude=coverage \
            ./client/ ~/megaweave/client/

          echo "📦 Client code deployed"

      - name: Install all dependencies for build
        run: |
          cd ~/megaweave/client
          npm ci

          # Make sure .env file exists
          if [ -f .env.production ]; then
            echo "✅ .env.production found"
          else
            echo "❌ .env.production not found!"
            exit 1
          fi
          echo "📥 Production dependencies installed"

      - name: Build client
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }} # 👈 將 Token 從 Secrets 傳入
          SENTRY_ORG: megaweaving
          SENTRY_PROJECT: javascript-nextjs
          # 限制 Node.js 記憶體使用
          NODE_OPTIONS: "--max-old-space-size=1536"
        run: |
          cd ~/megaweave/client

          # 清理舊的 .next
          rm -rf .next

          # 安裝依賴
          npm ci

          echo "🔨 Starting build..."
          free -h
          npm run build
          echo "📦 Client built successfully"
          free -h

      - name: Clean up dev dependencies
        run: |
          cd ~/megaweave/client
          # Build 完成後移除開發依賴
          npm ci --omit=dev
          echo "🧹 Cleaned up dev dependencies for production runtime"

      - name: Start frontend service
        run: |
          sudo systemctl start megaweave-fe.service
          echo "🚀 Frontend service started"

      - name: Verify frontend service
        run: |
          sleep 5
          if sudo systemctl is-active --quiet megaweave-fe.service; then
            echo "✅ Frontend service is running successfully"
          else
            echo "❌ Frontend service failed to start"
            # 顯示服務狀態和日誌
            sudo systemctl status megaweave-fe.service
            sudo journalctl -u megaweave-fe.service --lines=20 --no-pager
            exit 1
          fi

  post-deploy-cleanup:
    name: Post-Deploy Cleanup
    needs: [detect-changes, deploy-server, deploy-client]
    if: always() && needs.detect-changes.outputs.any-changed == 'true'
    runs-on: self-hosted
    steps:
      - name: Clean up workspace
        run: |
          echo "🧹 清理 Actions 工作空間..."

          # 等待一下確保所有服務都已啟動
          sleep 3

          # 只清理當前 repository 的工作目錄（更精確的路徑）
          WORK_DIR="${GITHUB_WORKSPACE}"
          if [ -d "$WORK_DIR" ]; then
            echo "清理當前工作目錄: $WORK_DIR"
            cd ~  # 先離開工作目錄
            rm -rf "$WORK_DIR/server/node_modules" 2>/dev/null || true
            rm -rf "$WORK_DIR/client/node_modules" 2>/dev/null || true
            rm -rf "$WORK_DIR/client/.next" 2>/dev/null || true
          fi

          # 清理 npm 快取
          npm cache clean --force

          # 清理臨時文件
          sudo rm -rf /tmp/npm-* 2>/dev/null || true
          sudo rm -rf /tmp/v8-* 2>/dev/null || true

          # 清理 3 天前的工作目錄（更激進）
          find ~/github-runner/actions-runner/_work -mindepth 1 -maxdepth 1 -type d -mtime +3 -exec rm -rf {} \; 2>/dev/null || true

          echo "✅ 清理完成"

      - name: Final disk check
        run: |
          echo "📊 最終磁碟狀態："
          df -h /
          echo ""
          echo "💾 記憶體狀態："
          free -h

  deployment-summary:
    name: Deployment Summary
    needs:
      [
        detect-changes,
        create-backup,
        deploy-server,
        deploy-client,
        post-deploy-cleanup,
      ]
    if: always() && needs.detect-changes.outputs.any-changed == 'true' && !contains(needs.*.result, 'failure')
    runs-on: self-hosted

    steps:
      - name: Print deployment summary
        run: |
          echo "🎯 === DEPLOYMENT SUMMARY ==="
          echo "📅 Time: $(date)"
          echo "🔍 Changes detected:"
          echo "  - Server: ${{ needs.detect-changes.outputs.server-changed }}"
          echo "  - Client: ${{ needs.detect-changes.outputs.client-changed }}"
          echo ""
          echo "📦 Backup:"
          if [ "${{ needs.create-backup.outputs.backup-created }}" == "true" ]; then
            echo "  - ✅ Created: ${{ needs.create-backup.outputs.backup-path }}"
          else
            echo "  - ⚠️ No backup needed (fresh deployment)"
          fi
          echo ""
          echo "🚀 Deployment results:"
          if [ "${{ needs.deploy-server.result }}" != "" ]; then
            echo "  - Backend: ${{ needs.deploy-server.result }}"
          else
            echo "  - Backend: skipped (no changes)"
          fi
          if [ "${{ needs.deploy-client.result }}" != "" ]; then
            echo "  - Frontend: ${{ needs.deploy-client.result }}"
          else
            echo "  - Frontend: skipped (no changes)"
          fi
          echo ""
          echo "📊 System status:"
          df -h / | tail -1
          echo ""
          echo "🎉 Deployment completed successfully!"

  deployment-failure:
    name: Deployment Failed
    needs: [detect-changes, create-backup, deploy-server, deploy-client]
    if: always() && needs.detect-changes.outputs.any-changed == 'true' && contains(needs.*.result, 'failure')
    runs-on: self-hosted

    steps:
      - name: Print failure summary
        run: |
          echo "❌ === DEPLOYMENT FAILED ==="
          echo "📅 Time: $(date)"
          echo "🔍 Changes detected:"
          echo "  - Server: ${{ needs.detect-changes.outputs.server-changed }}"
          echo "  - Client: ${{ needs.detect-changes.outputs.client-changed }}"
          echo ""
          echo "💥 Failed jobs:"
          if [ "${{ needs.deploy-server.result }}" == "failure" ]; then
            echo "  - ❌ Backend deployment failed"
          fi
          if [ "${{ needs.deploy-client.result }}" == "failure" ]; then
            echo "  - ❌ Frontend deployment failed"
          fi
          echo ""
          echo "📊 Current system status:"
          df -h /
          free -h
          echo ""
          echo "🔧 Please check the job logs above for details"
