"use client";
import React from "react";

const AddToCart = () => {
  return (
    <div>
      <button
        onClick={() => {
          console.log("hello!");
        }}
      >
        click
      </button>
    </div>
  );
};

export default AddToCart;
