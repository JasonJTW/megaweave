{
  "name": "megaweave-image-resizer",
  "version": "1.0.0",
  "description": "AWS Lambda function to resize images uploaded to S3",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "esbuild index.ts --bundle --platform=node --target=node24 --format=esm --outfile=dist/index.mjs --external:sharp --external:@aws-sdk/*",
    "package": "rm -f function.zip && npm run type-check && npm run build && cd dist && zip -r ../function.zip index.mjs",
    "install:layer": "mkdir -p layer/nodejs && npm install --os=linux --cpu=x64 --prefix=layer/nodejs sharp",
    "package:layer": "npm run install:layer && cd layer && zip -r ../sharp-layer.zip nodejs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.600.0",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
